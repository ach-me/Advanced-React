// No es necesario importar React porque Next se encarga de eso
import Link from 'next/link';

// Stateless component
const Home = props => (
  <div>
    <p>Home!</p>
  </div>
);

export default Home;
